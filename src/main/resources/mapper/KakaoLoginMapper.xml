<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.VeterinaryClinic.Mapper.KakaoLoginMapper">

    <!-- 1) ID로 회원 조회 -->
    <select id="getKakaoMember" parameterType="string" resultType="com.example.VeterinaryClinic.DTO.MemberDTO">
        SELECT ID, NAME, MAIL, INS_USER, INS_TIME, MOD_USER, MOD_TIME
        FROM KAKAO_MEMBER
        WHERE ID = #{id}
    </select>

    <!-- 2) 회원 INSERT -->
    <insert id="insertKakaoMember" parameterType="com.example.VeterinaryClinic.DTO.MemberDTO">
        INSERT INTO KAKAO_MEMBER (ID, NAME, MAIL, INS_USER, MOD_USER)
        VALUES (#{id}, #{name}, #{mail}, #{insUser}, #{modUser})
    </insert>

</mapper>
